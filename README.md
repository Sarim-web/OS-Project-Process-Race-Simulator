# OS-Project-Process-Race-Simulator
"A dynamic, terminal-based simulation written in C++ that models a race between multiple independent processes; the simulation relies entirely on Shared Memory (IPC) to update and track each racer's position in real-time."
